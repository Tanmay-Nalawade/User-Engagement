# Vite dev (http://localhost:5173): leave empty — vite proxies /interests and /api to :8080
VITE_API_URL=

# Or call the API directly (requires API CORS on :8080):
# VITE_API_URL=http://localhost:8080
